# 目标

实现针对SC7A20H芯片的驱动，重点用于设备的休眠与唤醒。

# 环境
1. 编译使用的是KEIL MDK
2. 芯片为PY32F030x8
3. 软件API使用HAL/LL，优先使用LL库

## 要求
1. SPI驱动层和SC7A20H驱动层分离。SPI放置User/spi目录下，SC7A20H放置User/mems目录下。
2. 振动芯片抽出API,方便后续添加其他芯片。
3. 同时只会使用一种振动芯片，可通过宏定义来选择。
4. 主要是用于外部振动唤醒工作，唤醒引脚使用SC7A20H的INT1引脚，其他附属功能可暂时不实现。
5. 至于调试打印log，可使用printf()函数,请将printf封装成宏定义，可关闭可开启，比如release版本下关闭，debug版本下开启。
6. 请增加更多的log信息，方便调试和排查。

